{% extends 'base.html.twig' %}

{% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('assets/css/home.css') }}">
{% endblock %}

{% block body %}

        <div class="header">
                <a href="{{ path('app_home_home') }}"><img src="{{asset('/assets/picture/Logo YouFilm3.png')}}" alt="Logo YouFilm" class="logo"></a>
                <div class="div-button-header-disconnect">
                        <a href="{{ path('app_user_logout') }}">
                                <button class="btn btn-danger button-disconnect-header">Déconnexion</button>
                        </a>
                </div>
        </div>

        <div class="grid">
                <h2 class="mb-4">Ajouter un commentaire :</h2>

                {{ form_start(form) }}
                <div class="form-group">
                        {{ form_label(form.content, 'Contenu') }}
                        {{ form_widget(form.content, {'attr': {'class': 'form-field'}}) }}
                        {{ form_errors(form.content) }}
                </div>

                <div>
                        {{ form_row(form.submit, {'attr': {'class': 'btn-submit'}}) }}
                        <a href="{{ path('movie_show', {'id': movie.id}) }}" class="btn-cancel">Annuler</a>
                </div>
                {{ form_end(form) }}
        </div>

{% endblock %}
